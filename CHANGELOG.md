# CHANGELOG

All notable changes to this project will be documented in this file.

## [1.1.0] - 2026-09-11

### 🚀 新機能 (New Features)
- **イラスト特化型本格AI超解像（Real-ESRGAN-anime6B）の導入**:
  - Pure-Rust ディープラーニング推論エンジン `tract-onnx`（0.23）をバックエンドに統合。
  - イラスト・立ち絵専用モデル `realesrgan-anime6B.onnx`（約17.9MB）を `include_bytes!` で `.exe` 実行ファイル内部に直接ビルド埋め込み。
  - 外部の `models/` フォルダが一切不要な、単体1ファイル構成（シングルバイナリ）で完全スタンドアロン動作。
  - 小さい切り抜き画像（例: 400x300未満）の拡大時に、線画のボケやジャギーを消し、4倍超解像で鮮明かつ滑らかに復元・出力。
- **WebP 非可逆（Lossy）高画質エンコードの対応**:
  - Google公式 `libwebp` バインディング（`webp` クレート 0.3）をバックエンドに導入。
  - WebP保存時および「形式維持」で元画像がWebPの場合、エクスポートモーダルの品質指定（Quality: 1〜100%）に連動した非可逆（Lossy）圧縮を適用。
  - 切り抜き出力時にWebPが可逆保存されて大容量化する現象を解消し、高い画質を維持したまま数10KB〜100KB程度の軽量ファイルサイズで出力可能に。

### 🎨 仕様変更・UI改善 (Improvements)
- **エクスポート設定の簡素化・指定サイズリサイズ一本化**:
  - エクスポートモーダルから「原寸で出力」オプションを削除し、指定解像度への拡大・縮小リサイズ出力のみに一本化。
  - 各アスペクト比の初期ターゲット解像度（`width x height`）を、タスクキューの最初の同アスペクト比アイテムの切り抜き原寸サイズへ自動デフォルト設定。
- **アスペクト比プリセット順序の最適化 ＆ ショートカット連携**:
  - 利用頻度に合わせてプリセット順序を `3:4`, `9:16`, `1:1`, `4:3`, `16:9`, `Free` に再編。
  - キーボードショートカット（数字キー 1〜6）を新順序へ追従。
- **ヘッダー操作による一括アスペクト比変更 ＆ レスポンシブ表示閾値の最適化**:
  - ヘッダーでアスペクト比を変更した際、チェックマークのついていない未完了タスク（`isCompleted === false`）のアスペクト比を一括変更する機能を追加。
  - アスペクト比ボタン一覧からドロップダウンメニューへ切り替わるブレイクポイントを `xl` (1280px) から `lg` (1024px) へ変更。ウィンドウサイズを少し縮めても横並びボタン一覧が維持されるよう調整。

### ⚡ パフォーマンス＆安定性改善 (Performance & Stability)
- **キャンバスドラッグ操作の高速化・軽量化**:
  - `requestAnimationFrame`（rAF）による描画スロットリングを導入。
  - ドラッグ操作中はCSSトランジションおよびSVGドロップシャドウフィルタを時限バイパスし、フレームレートと追従性を向上。
- **ファイルドロップ時の二重登録防止**:
  - TauriネイティブドロップイベントとHTML5 Drag & Dropイベントの重複発火によるファイル二重追加を防ぐ排他デバウンス処理を実装。
